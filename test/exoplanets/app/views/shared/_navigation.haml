%nav#navigation
  %ul
    = nav_link for: :home, icon: :home, url: root_path, class: 'blue'

    - if current_user && current_user.has_role?(:admin)
      = nav_link for: :home, icon: :home, admin: true, url: admin_root_path, class: 'red'
